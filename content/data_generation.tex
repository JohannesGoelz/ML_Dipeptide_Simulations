
\section{Methods}

\subsection{Data Generation}
\label{subsec:data_generation}

A central component of the project is the generation of large-scale simulation data that can serve as training input for \gls{ml} models. In these simulations, solutes are solvated in a water box, equilibrated, and then propagated over time to obtain representative trajectories. Both coordinates and forces are stored, which allows the resulting datasets to be used in diverse training settings such as force-matching.

To improve sampling efficiency, \gls{remd} \cite{qi2018replica} is incorporated. By running simulations at multiple temperatures and exchanging configurations, \gls{remd} helps to explore conformational space. This is important in order to avoid biased datasets that overrepresent only a limited set of conformations.

The data pipeline is designed for large-scale simulation of amino acid sequences. 

The pipeline begins with the generation of three-dimensional solute structures from amino acid sequences using AmberTools \texttt{tleap}, which produces capped or uncapped PDB files depending on the desired terminus configuration. 
These structures are subsequently solvated in a TIP3P explicit water box using OpenMM's \texttt{Modeller}, with a padding around the solute to ensure a sufficiently large solvation shell. The AMBER14 force field is applied to parameterize all interactions. Prior to production, each system undergoes energy minimization followed by an NPT equilibration phase, during which the Monte Carlo barostat maintains a constant pressure to relax the solvation shell and achieve an equilibrium density. The equilibrated system is then handed to the Reform-based \gls{remd} framework, which initializes one replica per temperature along an automatically determined temperature ladder, calibrated to yield an exchange acceptance rate of approximately 30\%. All replicas are propagated simultaneously using Langevin dynamics. During production, a custom \texttt{RemdForceLogger} hook intercepts coordinates and forces for all replicas at every snapshot interval and writes them in buffered chunks to a compressed HDF5 file, avoiding memory exhaustion even for long trajectories. After the simulation completes, a post-processing step extracts the frames belonging to the lowest-temperature replica, retaining only the solute atoms and discarding the surrounding solvent, and saves the resulting data as a compressed \texttt{.npz} archive containing coordinate and force arrays. The entire workflow from sequence input to job submission is orchestrated by a dedicated pipeline script that constructs SLURM batch jobs and submits them to the cluster, enabling the parallel processing of many sequences.

The scope of data generation in this project is to assemble datasets for model development and benchmarking. While the current focus lies on explicit water with standard force fields, the approach can naturally be extended to other solvents in later stages.


\subsection{Computational Efficiency}
\label{subsec:throughput}

To evaluate the computational efficiency of our data generation pipeline, we measured the throughput using a model system consisting of an uncapped tetra-alanine peptide (AAAA). 
The fully solvated system comprised 1,682 atoms in total (of which 43 atoms were from the solute) surrounded by explicit water molecules with a 1.2~nm padding. 
All benchmarking runs were executed on a single NVIDIA A100 GPU.

First, we assessed the throughput of conventional simulations at a constant temperature of 300~K (Figure~\ref{fig:throughput_300k}). 
To maximize GPU utilization, we simulated multiple independent copies of the system concurrently, dispatching each run in isolation via its own dedicated system process. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{images/throughput_300K.pdf}
    \caption{Aggregate simulation throughput for independent AAAA systems running concurrently at 300~K on a single NVIDIA A100 GPU.}
    \label{fig:throughput_300k}
\end{figure}

We also evaluated using REMD for the data generation.

For the \gls{remd} protocol, we leveraged the \texttt{reform} package \cite{TODO_add_reform_cite} to orchestrate replica swaps. 
By default, the multi-threaded interface of \texttt{reform} does not support periodic boundary conditions. 
However, modeling the explicit solvent relies on periodic boundary conditions to avoid surface artifacts. 
To overcome this limitation, we constructed a custom wrapper around \texttt{reform}'s single-threaded API. 
By managing a dedicated thread for each replica, we parallelized replica executions while retaining the necessary support for periodic boundary conditions. 
The throughput for our customized \gls{remd} implementation is outlined in Figure~\ref{fig:throughput_reform}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{images/throughput_reform.pdf}
    \caption{Throughput of our custom-parallelized \gls{remd} setup using the \texttt{reform} package, evaluated on the uncapped AAAA model system.}
    \label{fig:throughput_reform}
\end{figure}

In the end, we did not use REMD for the final data generation, as the computational overhead of running multiple replicas does not justify the improved sampling.


\subsection{Production Datasets}
\label{subsec:production_datasets}

To develop and rigorously evaluate our models, we orchestrated large-scale production simulations across both dipeptides and tetrapeptides. In keeping with the objective of generating clean, comparable snapshots, all baseline equilibration and environment variables were standardized across datasets. Specifically, each solvated system was padded with 1.2\,nm of explicit water and simulated at a constant ambient temperature of 300\,K. Prior to production sampling, every sequence underwent a comprehensive 100\,ns isothermal-isobaric (NPT) equilibration phase to establish appropriate system densities, followed directly by a 100\,ns canonical (NVT) equilibration. Data was then mined uniformly, extracting exactly 2,000,000 frames evenly from each individual production run independent of the simulation length.

Our primary investigation centered on dipeptides, exploring the conformational space of 400 distinct sequences. This cohort was strictly partitioned into a robust 280-peptide training partition and a separate 120-peptide testing partition. Every dipeptide sequence in this collection was simulated continuously for 1\,\textmu s. 

To subsequently interrogate the transferability and predictive capability of our learned representations on larger, more demanding biomolecules, we expanded our data schema with tetrapeptides. The tetrapeptide training set aggregates 1,500 varied sequences, each pushed through a 500\,ns production simulation. For robust benchmarking, we additionally formulated a highly concentrated tetrapeptide test set comprising just 30 strategically selected sequences. To ensure highly faithful reference distributions for test evaluations, these 30 evaluation systems were subjected to extraordinarily deep 5\,\textmu s simulations. A comprehensive breakdown of the structural parameters, runtimes, and scale of each corpus is detailed in Table~\ref{tab:production_datasets}.

\begin{table*}[htbp]
    \centering
    \begin{tabular}{l c c r r r r r}
        \toprule
        \textbf{Dataset} & \textbf{Temp.} & \textbf{Padding} & \textbf{NPT Eq.} & \textbf{NVT Eq.} & \textbf{Peptides} & \textbf{Sim. Time} & \textbf{Frames} \\
        \midrule
        Dipeptides (Train) & 300\,K & 1.2\,nm & 100\,ns & 100\,ns & 280 & 1\,\textmu s & 2,000,000 \\
        Dipeptides (Test) & 300\,K & 1.2\,nm & 100\,ns & 100\,ns & 120 & 1\,\textmu s & 2,000,000 \\
        Tetrapeptides (Train) & 300\,K & 1.2\,nm & 100\,ns & 100\,ns & 1,500 & 500\,ns & 2,000,000 \\
        Tetrapeptides (Test) & 300\,K & 1.2\,nm & 100\,ns & 100\,ns & 30 & 5\,\textmu s & 2,000,000 \\
        \bottomrule
    \end{tabular}
    \caption{Overview of simulation parameters, phase durations, and sequence composition across all primary dataset partitions.}
    \label{tab:production_datasets}
\end{table*}
\subsection{Model Architecture}

\subsection{Simulation Framework}